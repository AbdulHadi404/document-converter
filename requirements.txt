# Document Processing Libraries
pdfplumber==0.11.0       # PDF processing (easier to install, no C++ compiler needed)
python-docx==1.1.0       # DOCX processing (fallback)
mammoth==1.6.0           # DOCX to Markdown (fallback when Pandoc unavailable)
Pillow==10.1.0           # Image processing
pytesseract==0.3.10      # OCR engine
pdf2image==1.16.3        # Convert PDF pages to images for OCR

# Additional utilities
tqdm==4.66.1             # Progress bars (optional enhancement)

# Note: Also requires system tools:
# - Pandoc (for best DOCX conversion quality)
# - Tesseract OCR (for scanned PDFs)
# - Poppler (for PDF to image conversion)

